'use client';
import React from 'react';

export default function AdminIndexWrapper(){
  return <div style={{padding:20}}><h2>Admin (migrated)</h2><p>Admin routes are available under /admin/* (migrated to App Router).</p></div>;
}
