'use client';
import React from 'react';

export default function AdminLoginWrapper(){
  return <div style={{padding:20}}><h3>Admin - Login (migrated)</h3><p>Use the admin login form here (migrated to App Router).</p></div>;
}
